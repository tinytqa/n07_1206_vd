pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Cloning source code'
                git branch: 'main', url: 'https://github.com/tinytqa/n07_1206_vd'
            }
        }

        stage('Publish') {
            steps {
                echo 'Publish to running folder'
                // Stop IIS to avoid file conflict (you can uncomment the next line if needed)
                // bat 'iisreset /stop'

                bat 'xcopy "%WORKSPACE%\\publish" "c:\\n071206" /E /Y /I /R'
            }
        }
    }
}
